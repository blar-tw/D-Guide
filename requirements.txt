# Python dependencies for D-Guide (ROS 2 Humble itself is installed via apt).
# Install with: pip install -r requirements.txt

# Path planning (Google Maps Directions / Geocoding)
googlemaps>=4.10
requests>=2.28

# Flight control (MAVLink / Pixhawk / SITL)
dronekit>=2.9.2
pymavlink>=2.4

# Voice interface (wake word + speech-to-text)
pvporcupine>=3.0
pvrecorder>=1.2
google-cloud-speech>=2.20
pyaudio>=0.2.13

# LLM command parsing (optional; llm_node falls back to an offline
# rule-based parser when this or ANTHROPIC_API_KEY is missing)
anthropic>=0.40
